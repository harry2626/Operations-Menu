Instructions to compile and run the Project:

1. Open the command prompt.
2. Use the cd command to get the path of the folder.
3. Use the javac MainClass.java command to compile the source file.
4. Then, use the command java MainClass to run the class file.